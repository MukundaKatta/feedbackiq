{
  "name": "@feedbackiq/supabase",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:migrate": "supabase db push",
    "db:generate-types": "supabase gen types typescript --local > src/database.types.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.46.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "supabase": "^1.200.0"
  }
}
